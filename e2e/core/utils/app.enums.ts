export enum UserGroup {
    Admin = "admin",
}

export enum AppModules {
    Orders = "orders",
    Contacts = "contacts",
    SalesChannel = "sales_channel",
    Marketing = "marketing",
    Desing = "desing",
    Preferences = "preferences",
    MyAccount = "my_account",
}

export enum AppPanels {
    SetupShipping = "setup_shipping",
    SetupPayments = "setup_payments",
    StepsWebinar = "first_steps_webinar",
    Community = "community",
    SetupLayout = "setup_layout",
    SetupFirstProduct = "setup_first_product",
}

export enum MyAccountMenu {
    Logout,
}
